---
const { title, author, date, content } = Astro.props;
---

<div
  class="flex flex-col p-5 rounded-xl border border-gray-200 hover:bg-gray-200 hover:border-gray-300 hover:cursor-pointer transition-colors duration-200"
>
  <div class="mb-3">
    <h2 class="font-bold">{title}</h2>
    <p class="italic">
      <small>
        By {author} on {new Date(date).toLocaleDateString()}
      </small>
    </p>
  </div>
  <p>{content}</p>
</div>
